name: bilgici
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  firebase_storage: ^12.3.7
  cloud_firestore: ^5.5.1
  path_provider: ^2.1.5
  youtube_player_flutter: ^9.1.3
  google_fonts: ^6.3.3
  sqflite: ^2.3.0
  path: ^1.8.3
  shared_preferences: ^2.2.2
  lottie: ^3.3.0
  flutter_local_notifications: ^18.0.1
  intl: ^0.19.0
  # State Management & Architecture
  flutter_riverpod: ^2.6.1
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  # Optimizations
  cached_network_image: ^3.4.1
  flutter_secure_storage: ^9.2.2
  logger: ^2.5.0
  http: ^1.2.0
  # Games & Sensors
  shake: ^3.0.0
  sensors_plus: ^6.1.1
  confetti: ^0.7.0
  # Sinematik Animasyonlar
  flutter_animate: ^4.5.2
  archive: ^3.3.7
  timezone: ^0.9.2
  # Talking Mascot - Ses kaydı ve oynatma
  record: ^6.0.0
  just_audio: ^0.9.40
  permission_handler: ^11.3.1
  glass_kit: ^4.0.2
  provider: ^6.1.5+1
  font_awesome_flutter: ^10.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.3.0
  # Code Generation
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.9.2
  # Lints
  flutter_lints: ^5.0.0
  mockito: ^5.4.6
  # Tools - manifest generator için
  crypto: ^3.0.3

flutter:
  uses-material-design: true
  assets:
    - assets/json/
    - assets/images/
    - assets/animation/
